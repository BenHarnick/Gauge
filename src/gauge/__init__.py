"""Health insurance app: benefits engine and ML cost predictor.

Top-level package that bundles the cost-share calculator (`benefits`) with
the machine-learning annual cost predictor (`predictor`). The FastAPI app
in `api` exposes both.
"""
